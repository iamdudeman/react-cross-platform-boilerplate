import * as actionsFile from './src/actions';
import * as containersFile from './src/containers';
import * as storeFile from './src/store';

export const actions = actionsFile;
export const containers = containersFile;
export const store = storeFile.store;
